apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: health-os
  namespace: health-os
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: health-os.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: health-os
                port:
                  number: 80
